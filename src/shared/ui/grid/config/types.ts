import {customPropTypes, defaultPropTypes} from "@/shared/lib/component";

export const types = {
    ...defaultPropTypes,
    children: customPropTypes.children
}

export const GridItemPropTypes = {
    ...defaultPropTypes,
    children: customPropTypes.children
}